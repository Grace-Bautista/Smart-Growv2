{
  "rules": {
    ".read": false,
    ".write": false,
    "liveData": {
      "smartGrow01": {
        ".read": "auth != null",
        ".write": false
      }
    },
    "deviceCommands": {
      "smartGrow01": {
        "components": {
          "$component": {
            ".read": "auth != null",
            ".write": "auth != null && newData.child('requestedBy').val() == auth.uid && ($component == 'humidifier' || $component == 'uvLight' || $component == 'ventFan' || $component == 'refillPump')",
            ".validate": "newData.hasChildren(['commandId','requestedBy','issuedAt','ttlMs','status','desired']) && newData.child('commandId').isString() && newData.child('requestedBy').isString() && newData.child('issuedAt').isNumber() && newData.child('ttlMs').isNumber() && newData.child('ttlMs').val() > 0 && newData.child('ttlMs').val() <= 30000 && newData.child('status').val() == 'pending' && (($component == 'refillPump' && newData.child('desired').hasOnly(['mode']) && newData.child('desired').child('mode').isString() && (newData.child('desired').child('mode').val() == 'auto' || newData.child('desired').child('mode').val() == 'on' || newData.child('desired').child('mode').val() == 'off')) || ($component != 'refillPump' && newData.child('desired').isBoolean()))"
          }
        }
      }
    }
  }
}
