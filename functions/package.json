{
  "name": "smart-grow-functions",
  "description": "Firebase Cloud Functions for Smart Grow auth and user management",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "seed:history": "node history_seed.js",
    "seed:history:cleanup": "node history_seed.js --cleanup"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  }
}
